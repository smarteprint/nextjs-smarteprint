export const metadata = {
  title: 'Settings Management',
  robots: {
    index: false,
    follow: false,
  },
};

export default function SettingsManagementLayout({ children }) {
  return children;
}
